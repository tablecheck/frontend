{
  "$schema": "http://json-schema.org/schema",
  "$id": "TsFileTypes",
  "title": "Add typescript file types definition",
  "type": "object",
  "properties": {
    "svgAsComponent": {
      "type": "boolean",
      "description": "",
      "x-prompt": "Should `import name from 'name.svg'` result in a react component (using plugins/transforms) or a file path?"
    }
  }
}
