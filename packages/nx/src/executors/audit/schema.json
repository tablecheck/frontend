{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Audit executor",
  "description": "",
  "type": "object",
  "properties": {
    "updatePrompts": {
      "type": "boolean",
      "description": "Run the audit with prompts to update or ignore vulnerable packages",
      "x-prompt": "Run the audit with prompts to update or ignore vulnerable packages? (set to false on the CI)"
    }
  },
  "required": ["updatePrompts"]
}
