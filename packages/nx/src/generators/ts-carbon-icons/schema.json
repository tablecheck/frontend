{
  "$schema": "http://json-schema.org/schema",
  "$id": "TsCarbonIcons",
  "title": "Generate a typescript definition file for @carbon/icons",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project where the definition will be located.",
      "x-prompt": "Which project will this definition file be located in?",
      "x-dropdown": "projects"
    }
  },
  "required": ["project"]
}
