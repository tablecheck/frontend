import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="scripts/commands/build-ssr" />

## build-ssr

### Deprecated

Use `razzle build` directly instead, this command aliases the razzle one but doesn't pass any arguments.

This builds an SSR javascript application into the `project/build` folder. See [start-ssr](#section-start-ssr) for more details.
